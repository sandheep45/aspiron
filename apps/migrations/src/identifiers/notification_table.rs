pub enum NotificationEventsTableIdentifier {
    Table,
    Id,
    UserId,
    Type,
    Payload,
    ScheduledAt,
}

pub enum NotificationLogsTableIdentifier {
    Table,
    Id,
    EventId,
    Status,
    SentAt,
}
