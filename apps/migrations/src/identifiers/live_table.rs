pub enum LiveSessionTableIdentifier {
    Table,
    Id,
    TopicId,
    ScheduledAt,
    DurationMin,
    Provider,
    JoinUrl,
}

pub enum LiveSessionLiveRecordingIdentifier {
    Table,
    Id,
    SessionId,
    RecordingUrl,
    CreatedAt,
}
