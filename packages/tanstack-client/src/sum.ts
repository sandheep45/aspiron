/**
 * Typesafe sum function that adds two numbers (Tanstack version)
 * @param a - First number
 * @param b - Second number
 * @returns The sum of a and b
 */
export function sum(a: number, b: number): number {
  return a + b
}
